# RotIDE Screenshots

This directory is populated by:

```bash
make docs-media
```

The generator uses VHS and writes PNG files for the screenshots linked from the
main README. The PNG files are intended to be committed after regeneration.
