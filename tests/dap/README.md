# DAP Test Fixtures

`tests/dap/` stores fixtures for manual debugger (DAP) smoke testing.

- `tests/syntax/` is for highlighting and parse behavior.
- `tests/lsp/` is for language-server features.
- `tests/dap/` is for launch/step/breakpoint/stack/variable/thread workflows.

Current fixture groups:

- `supported/c/`

